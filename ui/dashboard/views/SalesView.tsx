import { useState } from 'react';
import { StyleSheet, View } from 'react-native';
import { MaterialCommunityIcons } from '@expo/vector-icons';
import { Button, Card, IconButton, List, ProgressBar, Text } from 'react-native-paper';
import { colors } from '@/theme';
import type { Inventory } from '@/game/inventory';
import type { Market } from '@/game/market';
import { RESEARCH_PROJECTS, type ResearchChainId, type ResearchLedger, type ResearchProjectDefinition, type ResearchProjectId } from '@/game/research';
import type { ResearchAvailability } from '@/game/core/stores';
import { calculateSalesOrderAcquisitionDetails, calculateSalesOrderEstimatedWaitMinutes, calculateSalesOrderMarketComparison, getSalesResourceProfile, SALES_CUSTOMER_DOMAIN_PROFILES, SALES_CUSTOMER_TYPE_PROFILES, type SalesOrders } from '@/game/sales';
import { calculateSalesOrderPrestige } from '@/game/prestige';
import { getResourceIcon } from '@/game/resources';
import { formatCurrency, formatNumber } from '@/utils';
import { APP_ICONS, ECONOMY_PHASE_ICONS, SALES_CUSTOMER_DOMAIN_ICONS, SALES_CUSTOMER_TYPE_ICONS } from '@/icons';
import { DetailRow, SectionHeading } from '@/ui/dashboard/components/DashboardPrimitives';
import { styles } from '@/ui/dashboard/helpers/dashboard.styles';

const SALES_RESEARCH_CHAINS: readonly ResearchChainId[] = ['sales-capacity', 'sales-order-value-limit', 'sales-targeting', 'bid-value', 'local-market-network', 'market-diffusion-network'];

export function SalesView({ companyPrestige, customerPipelineProgress, currentGameTimeMs, economyPhase, fulfillSalesOrder, getResearchAvailability, inventory, market, maximumOpenOrders, rejectSalesOrder, research, salesOrders, startResearch }: { companyPrestige: number; customerPipelineProgress: number; currentGameTimeMs: number; economyPhase: 'crash' | 'recession' | 'stable' | 'expansion' | 'boom'; fulfillSalesOrder: (id: string) => boolean; getResearchAvailability: (projectId: ResearchProjectId) => ResearchAvailability; inventory: Inventory; market: Market; maximumOpenOrders: number; rejectSalesOrder: (id: string) => boolean; research: ResearchLedger; salesOrders: SalesOrders; startResearch: (projectId: ResearchProjectId) => boolean }) {
  const [salesList, setSalesList] = useState<'open' | 'closed'>('open');
  const [closedFilter, setClosedFilter] = useState<'fulfilled' | 'rejected' | 'expired'>('fulfilled');
  const [salesResearchExpanded, setSalesResearchExpanded] = useState(false);
  const openOrders = salesOrders.getOfferedOrders(); const closedOrders = salesOrders.getCompletedOrders(); const count = openOrders.length;
  const hasEligibleInventory = Object.entries(inventory.toSnapshot().entries).some(([resourceType, entry]) => entry.quantity >= getSalesResourceProfile(resourceType as import('@/game/resources').ResourceType).standardOrderLot);
  const acquisition = calculateSalesOrderAcquisitionDetails({ openOrderCount: count, companyPrestige, economyPhase, hasEligibleInventory });
  const hasPipelineOverflow = customerPipelineProgress > 1; const pipelineOverflowProgress = customerPipelineProgress % 1;
  const orders = salesList === 'open' ? openOrders : closedOrders.filter((order) => order.status === closedFilter);
  const salesResearch = SALES_RESEARCH_CHAINS.map((chainId) => RESEARCH_PROJECTS.filter((project) => project.chainId === chainId).sort((left, right) => left.tier - right.tier).find((project) => !research.hasCompleted(project.id)) ?? null).filter((project): project is ResearchProjectDefinition => project !== null);
  const economyColor = economyPhase === 'stable' ? colors.muted : economyPhase === 'crash' || economyPhase === 'recession' ? colors.error : colors.marketGreen;
  return <>
    <SectionHeading eyebrow="SALES" title="Customer orders" subtitle="Inventory-ready orders are shaped by customer type, relationships, prestige, and the economy." />
    <Card mode="contained" style={styles.featureCard}><Card.Content style={styles.cardContent}><Text style={styles.cardKicker}>CUSTOMER ACQUISITION</Text><Text variant="titleMedium">{hasEligibleInventory ? `${formatNumber(acquisition.chance * 100, { smartDecimals: true })}% / min · ~${formatNumber(calculateSalesOrderEstimatedWaitMinutes(acquisition.chance), { smartDecimals: true })} min` : 'Inventory needed for offers'}</Text><View style={localStyles.factorRow}><Factor description="Prestige discovery" icon={APP_ICONS.prestige} value={acquisition.prestigeDiscoveryMultiplier} /><Factor description="Open-order capacity" icon={APP_ICONS.openOrders} value={acquisition.pendingMultiplier} /><Factor color={economyColor} description={`${economyPhase} economy`} icon={ECONOMY_PHASE_ICONS[economyPhase]} value={acquisition.economyMultiplier} /></View><View accessibilityLabel={`Customer pipeline ${formatNumber(Math.min(customerPipelineProgress, 1) * 100, { decimals: 0 })}%`} style={styles.customerPipelineProgressTrack}><ProgressBar accessible={false} color={colors.primary} progress={Math.min(customerPipelineProgress, 1)} style={styles.customerPipelineProgressBar} />{hasPipelineOverflow && <View pointerEvents="none" style={[styles.customerPipelineOverflow, { width: `${pipelineOverflowProgress * 100}%` }]} />}</View></Card.Content></Card>
    <DetailRow label="Open orders" value={`${formatNumber(count)} / ${formatNumber(maximumOpenOrders)}`} />
    <Card mode="contained" style={styles.featureCard}><List.Accordion expanded={salesResearchExpanded} left={(props) => <List.Icon {...props} icon={APP_ICONS.research} />} onPress={() => setSalesResearchExpanded((current) => !current)} title="Quick sales research" titleStyle={localStyles.quickResearchTitle} description={`${formatNumber(salesResearch.length)} next projects`}>
      {salesResearch.map((project) => { const availability = getResearchAvailability(project.id); const icon = project.chainId === 'bid-value' ? APP_ICONS.bid : project.chainId === 'sales-order-value-limit' ? 'scale-balance' : project.chainId === 'local-market-network' ? APP_ICONS.localMarket : project.chainId === 'market-diffusion-network' ? 'transit-connection-variant' : APP_ICONS.contracts; return <List.Item key={project.id} title={project.name} description={availability.startable ? `${formatCurrency(availability.cost)} · ready to start` : availability.unmetReasons[0] ?? 'Requirements not met'} left={(props) => <List.Icon {...props} icon={icon} />} right={() => <Button compact disabled={!availability.startable} onPress={() => startResearch(project.id)}>Start</Button>} />; })}
    </List.Accordion></Card>
    <View style={styles.salesFilters}><Button mode={salesList === 'open' ? 'contained' : 'outlined'} onPress={() => setSalesList('open')} style={styles.salesFilterButton}>{`Open (${count})`}</Button><Button mode={salesList === 'closed' ? 'contained' : 'outlined'} onPress={() => setSalesList('closed')} style={styles.salesFilterButton}>{`History (${closedOrders.length})`}</Button></View>
    {salesList === 'closed' && <View style={styles.salesFilters}>{(['fulfilled', 'rejected', 'expired'] as const).map((filter) => <Button key={filter} mode={closedFilter === filter ? 'contained' : 'outlined'} onPress={() => setClosedFilter(filter)} style={styles.salesFilterButton}>{`${filter[0].toUpperCase()}${filter.slice(1)}`}</Button>)}</View>}
    {orders.length === 0 ? <DetailRow label={salesList === 'open' ? 'Open orders' : 'Order history'} value={salesList === 'open' ? 'No requests yet' : `No ${closedFilter} orders yet`} /> : orders.map((order) => { const canFulfill = order.lines.every((line) => inventory.getAmount(line.resourceType) >= line.quantity); const comparison = salesList === 'open' ? calculateSalesOrderMarketComparison(order, (resourceType) => market.getLocalPrice(resourceType)) : null; const expiresInMinutes = Math.max(0, order.expiresAtGameTimeMs - currentGameTimeMs) / 60_000; const prestigeReward = calculateSalesOrderPrestige(order.reward, order.premiumPercent); const comparisonColor = comparison && comparison.gain >= 0 ? colors.marketGreen : colors.error; return <Card key={order.id} mode="contained" style={styles.featureCard}><Card.Content style={styles.cardContent}><View style={styles.salesContractHeader}><View><Text variant="titleMedium">{order.customerName}</Text><View style={localStyles.iconRow}><MaterialCommunityIcons color={colors.muted} name={SALES_CUSTOMER_DOMAIN_ICONS[order.customerDomain] as never} size={14} /><Text style={styles.cardDescription}>{SALES_CUSTOMER_DOMAIN_PROFILES[order.customerDomain].label}</Text><MaterialCommunityIcons color={colors.muted} name={SALES_CUSTOMER_TYPE_ICONS[order.customerType] as never} size={14} /><Text style={styles.cardDescription}>{SALES_CUSTOMER_TYPE_PROFILES[order.customerType].label}</Text></View></View><Text style={styles.salesReward}>{formatCurrency(order.reward)}</Text></View>{order.lines.map((line, index) => <View key={line.resourceType} style={localStyles.iconRow}><Text style={styles.salesAvailability}>{`${getResourceIcon(line.resourceType)} ${formatNumber(line.quantity)}`}</Text><MaterialCommunityIcons color={colors.muted} name={APP_ICONS.bid} size={15} /><Text style={styles.salesAvailability}>{`${formatCurrency(line.bidUnitPrice)} / unit`}</Text>{comparison && index === 0 && <><MaterialCommunityIcons color={comparisonColor} name={APP_ICONS.localMarket} size={15} /><Text style={[localStyles.localComparisonText, { color: comparisonColor }]}>{`${comparison.gain >= 0 ? '+' : ''}${formatCurrency(comparison.gain)} (${comparison.gainPercent >= 0 ? '+' : ''}${formatNumber(comparison.gainPercent, { smartDecimals: true })}%)`}</Text></>}</View>)}{salesList === 'open' && <><View style={localStyles.iconRow}><MaterialCommunityIcons color={colors.muted} name={APP_ICONS.relationship} size={15} /><Text style={styles.salesComparison}>{`${formatNumber(salesOrders.getCustomerState(order.customerId, currentGameTimeMs, companyPrestige).relationship, { smartDecimals: true })} · ${formatNumber(expiresInMinutes, { smartDecimals: true })} min`}</Text><MaterialCommunityIcons color={colors.muted} name={APP_ICONS.prestige} size={15} /><Text style={styles.salesComparison}>{`+${formatNumber(prestigeReward, { smartDecimals: true })}`}</Text></View><IconButton accessibilityLabel={`Reject order for ${order.customerName}`} icon={APP_ICONS.close} iconColor={colors.error} onPress={() => rejectSalesOrder(order.id)} size={20} style={styles.salesRejectButton} />{!canFulfill && <Text style={styles.salesComparisonNegative}>Insufficient inventory</Text>}<View style={styles.salesActions}><Button accessibilityLabel={`Fulfill order for ${order.customerName}`} disabled={!canFulfill} mode="contained" onPress={() => fulfillSalesOrder(order.id)}>Fulfil order</Button></View></>}{salesList === 'closed' && <Text style={styles.salesAvailability}>{order.status === 'fulfilled' ? 'Fulfilled' : order.status === 'expired' ? 'Expired — relationship reduced' : 'Rejected'}</Text>}</Card.Content></Card>; })}
  </>;
}

function Factor({ color = colors.muted, description, icon, value }: { color?: string; description: string; icon: string; value: number }) { return <View accessibilityLabel={`${description}: ${formatNumber(value * 100, { smartDecimals: true })}%`} style={localStyles.factor}><MaterialCommunityIcons color={color} name={icon as never} size={14} /><Text style={[styles.salesAvailability, { color }]}>{`${formatNumber(value * 100, { smartDecimals: true })}%`}</Text></View>; }

const localStyles = StyleSheet.create({
  factor: { alignItems: 'center', flexDirection: 'row', gap: 3 },
  factorRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  iconRow: { alignItems: 'center', flexDirection: 'row', flexWrap: 'wrap', gap: 4 },
  localComparisonText: { fontSize: 12, fontWeight: '700' },
  quickResearchTitle: { fontSize: 15, fontWeight: '700' },
});
